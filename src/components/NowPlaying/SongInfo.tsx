import React from 'react';
import ProgressBar from './ProgressBar';

const SongInfo = () => {
    return (
        <div>
        SongInfo
        <ProgressBar/>
        </div>
    )
}

export default SongInfo;